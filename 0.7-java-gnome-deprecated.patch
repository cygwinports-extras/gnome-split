Required for building with java-gnome-4.0.16.

--- origsrc/gnome-split-0.7/src/org/gnome/split/gtk/widget/AreaStatusIcon.java	2010-04-25 09:38:52.000000000 -0500
+++ src/gnome-split-0.7/src/org/gnome/split/gtk/widget/AreaStatusIcon.java	2010-06-17 19:30:45.985283800 -0500
@@ -117,6 +117,6 @@ public class AreaStatusIcon extends Stat
         }
 
         // Update the icon tooltip
-        this.setTooltip(builder.toString());
+        this.setTooltipText(builder.toString());
     }
 }
--- origsrc/gnome-split-0.7/src/org/gnome/split/gtk/widget/MainToolbar.java	2010-04-25 09:38:52.000000000 -0500
+++ src/gnome-split-0.7/src/org/gnome/split/gtk/widget/MainToolbar.java	2010-06-17 19:56:09.037397400 -0500
@@ -48,7 +48,7 @@ public class MainToolbar extends Toolbar
         this.insert(assistants, 0);
 
         // Make this button a proxy of the assistant action
-        actions.getAction(ActionId.ASSISTANT).connectProxy(assistants);
+        assistants.setRelatedAction(actions.getAction(ActionId.ASSISTANT));
 
         // Attach a menu to this button
         final Menu menu = new Menu();
--- origsrc/gnome-split-0.7/src/org/gnome/split/gtk/widget/SelectView.java	2010-04-25 09:38:52.000000000 -0500
+++ src/gnome-split-0.7/src/org/gnome/split/gtk/widget/SelectView.java	2010-06-17 19:57:23.960682700 -0500
@@ -69,7 +69,7 @@ public class SelectView extends HBox
 
         // Connect the button as a proxy to the existing action
         action = app.getActionManager().getRadioAction(ActionId.SPLIT);
-        action.connectProxy(split);
+        split.setRelatedAction(action);
 
         // Merge action - switch to merge view
         this.merge = new RadioButton(group, _("Merge"));
@@ -77,7 +77,7 @@ public class SelectView extends HBox
 
         // Connect the button as a proxy to the existing action
         action = app.getActionManager().getRadioAction(ActionId.MERGE);
-        action.connectProxy(merge);
+        merge.setRelatedAction(action);
     }
 
     /**
